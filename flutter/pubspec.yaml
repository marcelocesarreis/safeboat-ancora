name: safeboat_ancora
description: SAFEBOAT — Âncora Virtual (anchor watch) com dispositivo de bordo simulado.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1   # Poppins (identidade visual SAFEBOAT); cai p/ system font offline

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
